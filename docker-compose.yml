version: "3"
services:
  frontend:
    image: frontend
    build: .


  proxy:
    image: proxy
    build: ./proxy
    ports:
        - "80:80"
    depends_on:
      - frontend
      
volumes:
  dbdata:
